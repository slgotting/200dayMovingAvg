You will have to run the db_create file first so that you have the databases to enter into.

There are two parts in the file that reads  '** path to your quotes.csv file downloaded
by the site **'. This is where you put the full path to your quotes.csv file that you
download when you open the browser for the yahoo API. 
